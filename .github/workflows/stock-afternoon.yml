name: Stock Afternoon Report (尾盘 14:20)

on:
  schedule:
    - cron: '20 6 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run stock notifier + process trades
        env:
          PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
          TRADE_TOKEN: ${{ secrets.TRADE_TOKEN }}
          PERIOD: 尾盘
        run: python3 stock_notifier.py

      - name: Commit updated config (if changed)
        run: |
          git diff --quiet holdings_config.json pending_trades.json && exit 0
          git config user.name "stock-bot"
          git config user.email "stock-bot[bot]@users.noreply.github.com"
          git add holdings_config.json pending_trades.json
          git commit -m "自动更新: 处理交易记录"
          git push
